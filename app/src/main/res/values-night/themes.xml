<resources>
    <!-- Base application theme. -->
    <style name="Theme.ViewResources" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/main</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/statusBarColor</item>
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorError">@color/accent</item>

        <!-- Custom attrs. -->
        <item name="toolbarTextColor">@color/main</item>
        <item name="toolbarIconsColor">@color/icon</item>
        <item name="cartToolbarColor">@color/background</item>
        <item name="contactsToolbarColor">@color/shade</item>
        <item name="actionButtonWithIconColor">@color/secondary</item>
        <item name="actionButtonWithIconTint">@color/background</item>
        <item name="appButtonColor">@color/primary</item>
        <item name="statusBarColor">@color/background</item>
        <item name="headline6DarkAccent">@color/tretiary</item>
        <item name="body2DarkAccent">@color/secondary</item>
        <item name="lightAccentImageButtonTint">@color/icon</item>
        <item name="textButtonPrimaryAccentTextColor">@color/main</item>
        <item name="inputFieldHintColor">@color/main</item>
        <item name="inputFieldIconColor">@color/icon</item>
        <item name="inputEditTextBgTint">@color/shade</item>
        <item name="textColorLightAccent">@color/background</item>
        <item name="textColorDarkAccent">@color/main</item>
        <item name="dialogueSignInBg">@color/background</item>
        <item name="textButtonColor">@color/primary</item>
    </style>
</resources>